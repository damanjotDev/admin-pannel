export const ROUTES = {
    ROOT: '/',

    // Main Routes
    HOME: '/home',

    // Auth Routes
    LOGIN: '/login',
    REGISTER: '/register',
    FORGOT_PASSWORD: '/forgot-password',
    RESET_PASSWORD: '/reset-password/:id',
} as const;
